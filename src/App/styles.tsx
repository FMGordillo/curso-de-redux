import styled from 'styled-components'

export const Main = styled.main`
  display: grid;
  padding: 32px 128px;
  overflow: auto;
`

export const Footer = styled.footer`
  font-size: 0.75rem;
  align-self: center;
  justify-self: center;
`
